<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      
      <div class="text-center">
        <% if params[:plan] == '2' %>
          <h2>Paid Membership</h2>
          <p>Sign up for a Paid (£15/month) Mamebrship account, and get access to the latest health research material.</p>
        <% else %>
          <h2>Free Membership</h2>
          <p>Sign up for Free Membership account, and get access to interesting health reserach material.</p>
        <% end %>
      </div>
      
      <div class="well">
        
        <% if params[:plan] == '2' %>
         <%= render "paid_form" %>
        <% else %>
          <%= render "free_form" %>
        <% end %>
        
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>